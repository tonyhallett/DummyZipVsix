name: Build vsix and upload on pull request - success
on:
  repository_dispatch:
    types: [Build vsix and upload on pull request - success]

jobs:
  log-client-payload:
    name: log client payload
    runs-on: windows-2019
    steps:
      - name: log client payload
        env:
            Client_PAYLOAD: ${{ github.event.input.workflowRunPayload }}
        run:
            echo "Client payload $Client_PAYLOAD"
      
      

